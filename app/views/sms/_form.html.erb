<%= form_for(@sm) do |f| %>
  <% if @sm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sm.errors.count, "error") %> prohibited this sm from being saved:</h2>

      <ul>
      <% @sm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :sender_phone_id %><br />
    <%= f.number_field :sender_phone_id %>
  </div>
  <div class="field">
    <%= f.label :recipient_phone_id %><br />
    <%= f.number_field :recipient_phone_id %>
  </div>
  <div class="field">
    <%= f.label :sent_at %><br />
    <%= f.datetime_select :sent_at %>
  </div>
  <div class="field">
    <%= f.label :app %><br />
    <%= f.text_field :app %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
